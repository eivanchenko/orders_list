version: '3'
services:

    # Application base image
    #
    # This image contains:
    #
    #  - PHP runtime
    #  - PHP extensions
    #
    base:
        # Specify a tag name for your base image here:
        image: eivanchenko/ordersapp:latest
        build: ./
